// Application configuration settings

export const config = {
  // Unreviewed page settings
  unreviewed: {
    // Default number of videos to show at once
    defaultVideosToShow: 3,
    // Maximum number of videos that can be shown when expanded
    maxVideosToShow: 10
  }
};

export default config;
